import React from 'react'

const TodoFooter = props => {
    return (
        <li className="list-group-item">
            {/* {props.todoCompleteCount}已完成 / {props.todoCount}总数 */}
        </li>
    );
}

export default TodoFooter;